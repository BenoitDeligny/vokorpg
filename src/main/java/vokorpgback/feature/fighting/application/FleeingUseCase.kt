package vokorpgback.feature.fighting.application

class FleeingUseCase {
    fun handle(): Boolean {
        // TODO
        // lancer 2 dés + agi > fighting power des adversaires (sous entendu ceux affronté -> nombre par agi)
        return false;
    }
}